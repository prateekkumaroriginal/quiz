# Runtime schemas validate network data

The learner distinguished JSON syntax parsing from object-shape validation and identified an ArkType schema as the mechanism that rejects valid JSON with incorrect fields. Future lessons can treat network responses as `unknown` and focus on designing and applying the shared contract.

## Evidence

When given a valid JSON object with the wrong shape, the learner correctly answered that the ArkType schema must catch it.
