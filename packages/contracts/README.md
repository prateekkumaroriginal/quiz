# Quiz contracts

Runtime schemas and TypeScript types shared by the Quiz API and Quiz SDK.

Most users should install the SDK instead:

```bash
pnpm add @prateekkumaroriginal/quiz-sdk
```

The SDK installs this package as a dependency and re-exports its public types. Install the contracts package directly only when building a compatible Quiz API or related tooling.
